{{#if (includes frontend "next")}}'use client';
{{/if}}
import { FC } from 'react';
import { ClipLoader } from 'react-spinners';

const Loader: FC = () => {
  return <ClipLoader color="#3498db" size={50} />;
};

export default Loader;
